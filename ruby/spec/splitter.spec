require File.join(File.dirname(__FILE__), 'spec_helper.rb')

require 'Splitter'

describe Splitter do

	it "should tell when a list is not splittable"
	it "should split a trivial list"
	it "should split the Ruby Quiz example"

end